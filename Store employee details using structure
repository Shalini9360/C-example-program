#include <stdio.h>

struct employee {
    int id;
    float salary;
};

int main() {
    struct employee e;
    scanf("%d %f", &e.id, &e.salary);
    printf("ID: %d Salary: %.2f", e.id, e.salary);
    return 0;
}
