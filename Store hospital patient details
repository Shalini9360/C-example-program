#include <stdio.h>

struct patient {
    int id;
    int age;
};

int main() {
    struct patient p;
    scanf("%d %d", &p.id, &p.age);
    printf("Patient ID: %d Age: %d", p.id, p.age);
    return 0;
}
