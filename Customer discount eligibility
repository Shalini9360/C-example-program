#include <stdio.h>
int main() {
    float amount;
    printf("Enter purchase amount: ");
    scanf("%f", &amount);
    if (amount >= 1000)
        printf("Discount available");
    else
        printf("No discount");
    return 0;
}
