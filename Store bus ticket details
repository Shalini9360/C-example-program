#include <stdio.h>

struct ticket {
    int ticketNo;
    float fare;
};

int main() {
    struct ticket t;
    scanf("%d %f", &t.ticketNo, &t.fare);
    printf("Ticket No: %d Fare: %.2f", t.ticketNo, t.fare);
    return 0;
}
